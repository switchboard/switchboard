#show_members
  %h4 List Management
  %p
    #show_member_list
      / - if @list.list_type == 'announcement'
      /  * checkbox indicates approved sender
      %ul#member_list
        %li
        = render :partial => 'list_row', :collection => @lists

