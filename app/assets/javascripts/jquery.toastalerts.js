// code to show toast alerts, currently turned off.

/*$(function () {
  var alert_msg = $('.alert').text();
  if (alert_msg != '') {
    $j().toastmessage('showToast', {
            text     : alert_msg,
            sticky   : true,
            position : 'top-right',
            type     : 'success',
            closeText: '',
        });

  }
});

*/
